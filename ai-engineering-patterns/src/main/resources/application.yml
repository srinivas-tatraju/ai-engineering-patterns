server:
  port: 8080

spring:
  application:
    name: ai-engineering-patterns

  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: llama3.1

    mcp:
      client:
        enabled: true
        type: SYNC

        streamable-http:
          connections:
            calculator-server:
              url: http://localhost:8081